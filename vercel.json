{
  "rewrites": [{ "source": "/(.*)", "destination": "/clean-project/$1" }],
  "buildCommand": "cd clean-project && npm install && npm run build",
  "outputDirectory": "clean-project/.next",
  "devCommand": "cd clean-project && npm run dev"
} 